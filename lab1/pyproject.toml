[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lab"
version = "0.0.1"
dependencies = [
    "ipykernel",
    "jupyterlab",
    "matplotlib",
    "numpy",
]

[tool.setuptools.packages.find]
where = ["src"]
